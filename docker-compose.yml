---
services:
  mysql80:
    image: mysql:8.0
    command: ["--server-id=1", "--log-bin=/var/lib/mysql/mysql-bin.log", "--binlog-format=row"]
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: meteor
      MYSQL_DATABASE: test_package
